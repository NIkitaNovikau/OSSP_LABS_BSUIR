psman, info, mkdir, touch, rm, rmdir, cd, cat, sort, head, tail, tee,
wc, chmod, lab03, ls, lsof, lsblk, lsusb, lscpu, ln, link, unlink, locale, iconv, kill, top, htop, ps, grep, diff,
env, file, stat, find, tar, gzip, more, less, printf, time, ...


mkdir - создать директорию mkdir -p создать директорию в директории 
rm - удалить файл rm -r удалить директорию -f удалить все с игнором ошибок
cd - перейти в директорию
cp - коп файл или директорию флаги такие же как у rm
{
ls - вывести содержимое директории
ls -l - вывод в колонку с доп инфой о файле
ls -t - сортировка по времени
ls -r - обратная сортировка
}

open - открыть файл 
tail  -1... -n - прочитать файл с конца
head -1 ... -n - читает файл сверху
cat - просмотр файла
touch ... - создать новый файл
cat > ... - создать и сразу записать 
find - найти файл 
mv - перемещение или переименовать файл или директорию
data - вывести текущую дату и время 
w - кто залогинен в системе
ps - вывести список процессов
kill - уничтожить процесс по сигналу
man - справочник по руководству
gzip - сжатие файл -k с сохранением 
gzip -d file.gz - восстановление оригинальной версии файла из сжатой версии
от -1 до -9 это степень сжатия
Если же вам нужно создать архив с несколькими файлами внутри, одной утилиты gzip будет явно мало. Для этой цели также понадобится утилита tar, с помощью которой можно создать архив с файлами,
после чего сжать этот архив с помощью утилиты gzip. Например, вы можете использовать следующую последовательность команд для создания архива с именем archive.tar.gz:
$ tar -cf archive.tar <имена файлов>
$ gzip archive.tar
locale - Чтобы просмотреть информацию о текущей установленной локали
Locale — это набор переменных среды, которые определяют язык, страну и настройки кодировки символов (или любые другие особые предпочтения варианта) для ваших приложений и сеанса оболочки в системе Linux.
Эти переменные среды используются системными библиотеками и локальными приложениями в системе.



чем отличается жесткая ссылка от символьнойой????
Символьная ссылка (сокр. «symlink» от англ «symbolic link«), в отличие от жесткой ссылки, указывает не на индексный номер файла, а на его имя (путь). 
В каком-то роде символьная ссылка является аналогом ярлыка в Windows-системах.
Символическая ссылка в отличие от жесткой имеет свой собственный номер индексного
дескриптора и имеет свой тип.


Виды unix подсистем????
Планировщик (Process Scheduler)
Контроллер памяти (Меmory Manager) 
Виртуальная файловая система (Virival Fi1e System, VFS) 
Сетевой интерфейс (Network Interface, NЕТ) 
Подсистема межпроцессорного взаимодействия (Inter-Process Соmmunication IPC) 


что такое точка монтирования???
Точка монтирования (англ. mount point) — это каталог или файл, с помощью которого обеспечивается доступ к новой файловой системе, каталогу или файлу. 


потоки ввода/вывода???
Ввод и вывод распределяется между тремя стандартными потоками:

    stdin — стандартный ввод (клавиатура),
    stdout — стандартный вывод (экран),
    stderr — стандартная ошибка (вывод ошибок на экран).

узнать что такое монтирование 
про ссылочки
поток ошибок посмотреть пример
почему ссылка занимает столько то байт и что важнее ссылка или файл на который ссылается ссылка
